apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.22.0 (955b78124)
  creationTimestamp: null
  labels:
    io.kompose.service: stack-nginx
  name: stack-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: stack-nginx
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert
        kompose.version: 1.22.0 (955b78124)
      creationTimestamp: null
      labels:
        io.kompose.network/net1: "true"
        io.kompose.service: stack-nginx
    spec:
      initContainers:
        - name: collectstatic
          image: docker-django-nginx-uwsgi_stack-django
          imagePullPolicy: Never
          command: ['python3.8', 'manage.py', 'collectstatic', '--no-input']
          volumeMounts:
            - name: static-back
              mountPath: /home/django_test/static/   
      containers:
        - image: docker-django-nginx-uwsgi_stack-nginx
          imagePullPolicy: Never
          name: stack-nginx
          ports:
            - containerPort: 80
          resources: {}
          volumeMounts:
            - mountPath: /home/django_test/static
              name: static-back
              readOnly: true
            - mountPath: /usr/app/public/assets
              name: static-front
              readOnly: true
      restartPolicy: Always
      volumes:
        - name: static-back
          persistentVolumeClaim:
            claimName: static-back
        - name: static-front
          persistentVolumeClaim:
            claimName: static-front
status: {}
